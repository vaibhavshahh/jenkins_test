version: 0.0
os: linux
files:
  - source: /
    destination: /home/ubuntu/portal/
hooks:
  AfterInstall:
    - location: AfterInstallHook.sh
      timeout: 300
      runas: root

  BeforeInstall:
    - location: BeforeInstallHook.sh
      timeout: 300
      runas: root
